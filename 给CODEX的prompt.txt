先完整读一遍仓库根目录的 AGENTS.md，特别是第五节「文案打磨任务的专用规则」。

【背景】
我是刚开始学 AI 的产品经理，没有技术背景。这个页面是我准备面试用的知识手册。
现在的问题是：有些句子写得太"行话"了，我第一遍读根本读不进去，得反复看好几遍。

【任务】
做一次纯文案打磨。逐条读术语卡里的文字，只改那些我作为小白会卡住的句子。

【边界 —— 这次绝对不动的东西】
- <style> 和 <script> 一个字都不要碰
- 三张 SVG 架构图里的任何内容都不要碰（坐标是手排的，改文字会撑破图）
- 不要增删元素，不要改任何 class 属性
- .def（"面试这样说"那一句）原则上不改。那是我要在面试里原话背出来的，
  就该是紧的。只有它本身有语病、或者里面有没解释过的行话时才动。

【怎么改才算改好】
把抽象换成具体 —— 加一个我能想象出画面的场景，或者把行话就地解释一句。
不是加形容词，不是把一句话拆成三句。
改完的字数不要超过原来的 1.3 倍，超了就说明在灌水。

【交付方式】
这次只做「抽屉 01 · 模型输入」这 14 条（术语 01 到 14），做完就停下来。
给我一份清单：改了哪几条、每条改动前后对照、以及原来那句话哪里会让小白卡住。
我看完确认了，再让你做下一个抽屉。

【验证】
改完必须跑：python3 check_handbook.py --copy-only
必须全绿。任何一项报错都说明你改越界了，回退重做，不要绕过检查。
